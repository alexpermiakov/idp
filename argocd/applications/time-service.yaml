apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: time-service
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/alexpermiakov/idp
    targetRevision: main
    path: helm-charts/standard-service # Platform's golden chart
    helm:
      releaseName: time-service
      values: |
        app:
          name: time-service
          image: your-registry/time-service:v1.0.0
          port: 1599

        resourceProfile: medium  # small, medium, or large

        replicaCount: 3

  destination:
    server: https://kubernetes.default.svc
    namespace: time-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

