apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: version-service-prod
  namespace: argocd
  # Production: NO Image Updater - manual promotion only via Git PR
spec:
  project: default

  sources:
    - repoURL: https://github.com/alexpermiakov/idp
      targetRevision: main
      path: helm-charts/standard-service
      helm:
        releaseName: version-service
        values: |
          app:
            name: version-service
            image: 864992049050.dkr.ecr.us-east-1.amazonaws.com/idp/gitinfo:v1.0.0
            port: 8080
          resourceProfile: large
          replicaCount: 3

  destination:
    server: https://kubernetes.default.svc
    namespace: version-service-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

